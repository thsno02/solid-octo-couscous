# Materialized sources

Source-specific frozen artifacts. arXiv stores TeX and selectors; GitHub stores commit-pinned semantic capsules, not repository contents. Source families execute in separate bounded worker pools.
