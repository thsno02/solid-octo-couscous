\begin{wrapfigure}{R}{0.36\textwidth}
    \centering
    \vspace{-10mm}
    \includegraphics[width=0.35\textwidth]{figures/experiments/memory_t5.png}
    \caption{\textbf{GPU VRAM consumption} during training. ENN's memory usage{\protect\footnotemark} is prohibitively high for very large models, while {\name} and KE can be trained on a single GPU. Figure~\ref{fig:memory_full} shows similar chart for GPT models.}
    \label{fig:memory_t5}
\end{wrapfigure}