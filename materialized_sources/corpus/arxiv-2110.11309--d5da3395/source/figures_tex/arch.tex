\begin{figure}
    \centering
    \includegraphics[width=\textwidth]{figures/diagrams/arch.png}
    \caption{The \name~architecture, consisting of two consecutive blocks, both initialized to compute the exact identity function. \textbf{Left.} The input to a \name~network is $\{\delta_{\ell+1},u_\ell\}$, the components of the rank-1 gradient. \textbf{Right.} A \name~network produces a new rank-1 update $\tilde\nabla_{W_\ell}$, which is added to weights $W_\ell$ to edit the model.}
    \label{fig:arch}
\end{figure}