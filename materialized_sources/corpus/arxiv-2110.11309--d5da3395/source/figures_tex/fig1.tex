\begin{figure}[b]
    \centering
    \includegraphics[width=\textwidth]{figures/diagrams/fig1.png}
    \caption{The proposed algorithm \name~enables editability by training a collection of MLPs to modify model gradients to produce \textit{local} model edits that do not damage model performance on unrelated inputs. \name~is efficient to train and apply edits, even for very large models, as shown in Section~\ref{sec:large-models}.}
    \label{fig:problem-method}
\end{figure}