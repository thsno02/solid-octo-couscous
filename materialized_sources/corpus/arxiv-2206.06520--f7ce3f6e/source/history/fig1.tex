\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{figures/png/fig1.png}
    \caption{\textit{Overview of {\name}}. 
    When evaluating new test inputs $x, x'$ after several edit examples $\{x^i_e, y^i_e\}$ have been applied, we first embed the test inputs and edit examples with $\feat$ to find the most relevant (closest in embedding space) edit example for each test input. If the nearest edit example is within a (learned) threshold distance to the test input, we return the output of the counterfactual model $\rep$, which conditions on both the test input and nearest neighbor edit example, instead of the base model.
    }
    \label{fig:method-overview}
\end{figure}