name: Materialize sources with bounded retries

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/materialize-sources-resilient.yml'

permissions:
  contents: write

concurrency:
  group: materialize-sources-resilient
  cancel-in-progress: false

jobs:
  materialize:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install validators
        run: python -m pip install --disable-pip-version-check PyYAML jsonschema

      - name: Normalize structurally invalid legacy source types
        run: python scripts/preflight_legacy_metadata.py

      - name: Materialize initial source batch with bounded retries
        run: python scripts/materialize_resilient_runner.py --config pipeline/materialization.yaml

      - name: Register generated interfaces
        run: python scripts/register_materialization.py

      - name: Validate generated artifacts
        run: |
          python scripts/validate_materialized.py
          python scripts/validate_raw_data.py
          python scripts/validate_docs.py

      - name: Commit materialized sources and migrated metadata
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add raw_data source_registry materialized_sources
          if git diff --cached --quiet; then
            echo 'No generated changes.'
            exit 0
          fi
          git commit -m 'Materialize P0 sources and normalize source registry'
          git pull --rebase origin main
          git push origin HEAD:main
